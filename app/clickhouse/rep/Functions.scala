package clickhouse.rep

import slick.lifted.{Rep, SimpleExpression, SimpleFunction}

object Functions {
//  def uniq(c: Rep[String]) =
//    SimpleFunction[Long]("uniq").apply(Seq(c))


  // val uniq = SimpleFunction().unary[String, Long]("uniq")

//  val myExpr = SimpleExpression.binary[Int, Int, Int] { (l, r, qb) =>
//    qb.sqlBuilder += '('
//    qb.expr(l)
//    qb.sqlBuilder += '+'
//    qb.expr(r)
//    qb.sqlBuilder += "+1)"
//  }

}
